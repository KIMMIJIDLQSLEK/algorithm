def solution(s):
    num_max=-1000000
    num_min=1000000
    
    for num in s.split(" "):
        if num_max<int(num):
            num_max=int(num)
        if num_min>int(num):
            num_min=int(num)
    return f"{num_min} {num_max}"
